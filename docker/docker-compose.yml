version: '3.7'
services:
  postgre:
    image: postgres:latest
    ports:
      - 5432:5432
    container_name: inasaku_postgres
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: inasaku
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: dbsamp